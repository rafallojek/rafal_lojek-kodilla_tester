package com.kodilla.homework_one;

public class AdverbialOfTime extends Adverbial {
    @Override
    protected String[] getWords() {
        return new String[]{
                "at dawn", "in the night", "every morning", "before noon", "after twilight",
                "once in a while", "when stars align", "at sunset", "in silence", "yesterday"
        };
    }
}
