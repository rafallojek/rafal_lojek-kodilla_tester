package com.kodilla.homework_one;

public class AdverbialOfManner extends Adverbial {
    @Override
    protected String[] getWords() {
        return new String[]{
                "gently", "violently", "silently", "without fear", "with joy",
                "in vain", "passionately", "randomly", "mysteriously", "reluctantly"
        };
    }
}
